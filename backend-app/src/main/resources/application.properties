# ==================================
# C?U HÌNH MÁY CH? (SERVER)
# ==================================
# C?ng mà ?ng d?ng Spring Boot s? ch?y
server.port=8080

# ==================================
# C?U HÌNH C? S? D? LI?U (POSTGRESQL)
# ==================================
# URL k?t n?i JDBC.
spring.datasource.url=jdbc:postgresql://localhost:5432/food_ordering_app

# Tên ng??i dùng CSDL
spring.datasource.username=postgres

# M?t kh?u CSDL
spring.datasource.password=whYlonElY0!

# =Main-Driver class
spring.datasource.driver-class-name=org.postgresql.Driver

# ==================================
# C?U HÌNH JPA/HIBERNATE
# ==================================
# ?ây là ph?n "k? di?u": Spring Boot s? t? ??ng
# ??c các @Entity (User, Order, v.v.) c?a b?n
# và T? ??NG t?o các b?ng trong CSDL
spring.jpa.hibernate.ddl-auto=update

# (Tùy ch?n) Hi?n th? các câu l?nh SQL trong log (r?t h?u ích ?? debug)
spring.jpa.show-sql=true

# (Tùy ch?n) ??nh d?ng các câu l?nh SQL cho d? ??c
spring.jpa.properties.hibernate.format_sql=true

# Ch? ??nh "lo?i" CSDL ?? Hibernate t?i ?u câu l?nh SQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ==================================
# C?U HÌNH REDIS
# ==================================
# (Gi? ??nh Redis c?a b?n c?ng ch?y ? local v?i c?ng m?c ??nh)
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Bu?c Spring Boot luôn ch?y file data.sql (cho PostgreSQL)
spring.sql.init.mode=always